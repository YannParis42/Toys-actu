<!DOCTYPE html>
<html>

<head>
  <meta charset="UTF-8">
  <title>{% block title %}Welcome!{% endblock %}</title>
  <link rel="icon"
    href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
  {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
  {% block stylesheets %}
  {{ encore_entry_link_tags('appBack') }}
  {% endblock %}

  {% block javascripts %}
  {{ encore_entry_script_tags('appBack') }}
  {% endblock %}
</head>

<body>

  <div class="wrapper">
    {% if is_granted('ROLE_ADMIN') %}

    {% include "back/common/_navbar_admin.html.twig" %}
    {% include "back/common/_sidebar_admin.html.twig" %}

    <div class="content-wrapper">
      {% block content %}
      {% endblock %}
    </div>

    {% include "back/common/_footer_admin.html.twig" %}

    {% else %}
    <div class="text-center m-5">
      <h2>Vous n'avez pas les droits!</h2>
      <a type="button" class="btn btn-danger m-4" href="{{ path('app_home') }}">Retour accueil</a>
    </div>
    {% endif %}
  </div>
</body>

</html>