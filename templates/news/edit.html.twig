{% extends 'front.html.twig' %}

{% block title %}Edit actus{% endblock %}

{% block content %}
<div class=" m-4">
    <h1>Modifier Actualité</h1>
    <hr>
    <a type="button" class="btn btn-primary" href="{{ path('app_news_user', {'id': app.user.id}) }}">Retour</a>
    <hr>

    <div class="edit m-auto p-4"> 
        {% if news.newsPhoto %}
        <h2>Image actuelle:</h2>
        <div class="card-img-top editImage">
            <img src="{{asset('uploads/photo/'~news.newsPhoto)}}"   alt="{{news.newsPhoto}}">
        </div>
        <hr>
        {% endif %}
        <div class="card-body">
            {{ include('news/_form.html.twig', {'button_label': 'Modifier'}) }}
            {{ include('news/_delete_form.html.twig') }}
        </div>
    </div>

</div>
{% endblock %}